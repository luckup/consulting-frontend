# Copy to .env.production before: npm run build

# Vercel Blob public store (after npm run vercel:blob:upload)
VITE_CDN_BASE_URL=https://kmbR6bCYbcA9kB47.public.blob.vercel-storage.com

# Contact form email service (optional overrides)
# VITE_EMAIL_SERVICE_URL=https://email-service-hazel-three.vercel.app/api/send/moonsofts-prod
# VITE_EMAIL_SERVICE_TOKEN=moonsofts-prod

# Optional: stable production URL for canonical tags, Open Graph URLs, and JSON-LD (no trailing slash)
# VITE_SITE_URL=https://moonsofts.com

# API (optional — only if you host a separate backend)
# VITE_API_URL=
